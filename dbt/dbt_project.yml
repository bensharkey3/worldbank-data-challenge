name: 'worldbank_data_challenge'
version: '1.0.0'

profile: 'worldbank_data_challenge'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  worldbank_data_challenge:
    +materialized: view
